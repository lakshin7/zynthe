# Test config with missing data files (INVALID - for testing preflight)

train:
  epochs: 3
  batch_size: 8
  lr: 5e-5

model:
  name: "bert-base-uncased"
  student_name: "distilbert-base-uncased"
  type: "transformer"
  tokenizer_name: "distilbert-base-uncased"

distillation:
  method: "kd_hinton"
  temperature: 2.0
  alpha: 0.5

data:
  train_path: "data/non_existent_train.jsonl"  # This file doesn't exist
  val_path: "data/non_existent_val.jsonl"      # This file doesn't exist

device:
  prefer_mps: true

output_root: "experiments"
seed: 42
